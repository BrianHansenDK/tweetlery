<div class="d-flex flex-column align-items-center justify-content-center">
  <h1>Welcome to Tweetlery</h1>
  <% if @user %>
    <p>Logged in as: <%= @user.email %></p>
    <%= button_to "Logout", logout_path, class: 'btn btn-danger', method: :delete %>
  <% end %>
</div>
